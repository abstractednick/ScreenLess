# ScreenLess admin

Forest-night operator console. Talks to `services/api` as `admin@screenless.app`.

```bash
npm install
npm run dev
```

[http://localhost:3000](http://localhost:3000) — password `screenless` after a seed.
