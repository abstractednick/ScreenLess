# Product showcase

High-fidelity phone UI used for documentation screenshots and for walking a recruiter through ScreenLess without an emulator.

```bash
npm install
npm run dev
```

- [http://localhost:5173](http://localhost:5173) landing
- `#/gallery` every screen
- `#/live` isolated live room
