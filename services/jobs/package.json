{
  "name": "@screenless/jobs",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "ScreenLess background workers — window lifecycle, streak maintenance, reminder fan-out.",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
