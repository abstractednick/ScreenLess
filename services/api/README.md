# ScreenLess API

Express + TypeScript + Prisma.

```bash
npm install
npx prisma generate
npx prisma db push
npm run seed
npm run dev
npm test
```

Read `docs/API.md` for the full surface. The file to steal for an interview is `src/streakEngine.ts`.
